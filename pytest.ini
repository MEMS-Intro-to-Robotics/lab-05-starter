[pytest]
# Collect only the repository checks. Without this, pytest searches ros2_ws/,
# where colcon's install/ symlinks point into /root (unreadable on the VM) and
# the tests generated by ros2 pkg create import modules the VM does not have.
testpaths = test_lab_5.py
